import React from 'react';

import './startComponent.css';

export default function StartComponent(props) {

return (
            <button onClick= {props.pickNumber}>New Game!</button>
    );
    
};
